# EARLY_MOVER V2 Shadow Contract v1.1

Ruleset `early-mover-v2-shadow-v1.1-explicit-support`; display name `EARLY_RELATIVE_MOVER_V2`; shadow only and never production eligible.

`EARLY_RELATIVE_MOVER != PROVEN_SECTOR_LEADER_IN_TIME`. `NO_TEMPORAL_LEADERSHIP_CLAIM_WITHOUT_FORWARD_SEQUENCE = TRUE`. The signal date is only the date on which the current shadow condition is observed. No future sector confirmation is generated or backwritten.

Eligibility remains immutable V1 EARLY_MOVER. Trend support uses V1-frozen R² boundaries 0.40/0.55. Continuity and pulse reuse R3-01 classes unchanged, including validity rules. Position is healthy at the existing V1 Leader threshold POS60 >= 0.60. These remain four independent dimensions without a score.

Economic INDUSTRY/THEME stabilization has priority and yields `SECTOR_STABILIZING`; economic membership with no signal yields `NO_SECTOR_SIGNAL` even when price-behavior STYLE memberships coexist. Price-behavior-only and unknown-only mappings are uncertain and are not independent sector evidence.

With complete data, stabilizing context produces `SECTOR_ALREADY_STABILIZING`; uncertain context produces `CONTEXT_UNCERTAIN`; high pulse or weak continuity without stabilization produces `SHORT_TERM_PULSE`. No-sector-signal rows satisfying all four confirmations are `EARLY_CORE`. A non-core row is `EARLY_SUPPORTED` only when trend is at least moderate or position is healthy; continuity and pulse preconditions cannot count again as the two supporting dimensions. Only core/supported set the shadow hit. Missing critical stock or sector context is `DATA_INSUFFICIENT`.

No external/network data, index OHLC, future outcome, backtest, temporal leadership claim, probability, recommendation, automated trading, fitted threshold, V1 priority change, or production publication is permitted.
