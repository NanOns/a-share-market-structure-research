# FOCUS-03 首个 REAL_FORWARD 发布阶段记录

> 当前结果：`FULL_PASS / REAL_FORWARD_HEAD_ACTIVE_AND_VISIBLE`。

| 字段 | 记录 |
|---|---|
| applicable_upgrade | `DAILY_FOCUS_TRACKER_FINAL_DESIGN_V2_1_20260923.md` 第 7、10、14、15、19 节；`FOCUS_03_INPUT_MANIFEST_PROGRESS_20260923.md`；`FOCUS_05_API_UI_PROGRESS_20260923.md`。 |
| stage_contract | `FOCUS_FIRST_FORWARD_PUBLICATION_V1`；Focus core 首次原子写入及 `FOCUS_CORE_HEAD_ACTIVATION_V2`；读验沿用 `FOCUS_READ_API_V1`。预检阻断定位阶段另按 `PHASE1_NORMALIZED_PAIR_V1` 与 `ARTIFACT_CATALOG_V1` 检查输入身份，只允许精确重登记验收收据所绑定的项目文件。 |
| scope | 只用交易日 `2026-09-23` 的已接受 PostgreSQL publication、V3/V3.3 heads、完整主交易日历、已锁定本地事实与既有版本化 contracts 构建 REAL_FORWARD Focus manifest；预检通过且身份/能力复核后，按 exact manifest digest 原子提交 Focus run/head。用户页面仅从有效 PostgreSQL Focus head 读取。normalized Parquet 若需修复目录身份，必须先匹配 Phase1 最终收据中的日期、状态及 normalized/factor 双文件摘要，再对该单一路径精确更新 artifact catalog。禁止回退 DuckDB/文件指针，不修改 TDX 输入。 |
| preflight_evidence | 只读来源 head 探针：publication `m4-51729396603ef27a36e4b0390302766d`、日期 `2026-09-23`、状态 `SUCCESS`；V3 run `research-a0d4a0877a1d46b3a59aa91085e27b49` 为 `COMPLETE`；V3.3 head `8d59680ccc5ffcd4bc8a3c8cd8ddaca8ac2d84770584e4b02a414b87b6c8618c` 为 `COMPLETE`、273 行。Focus 只读 API 对默认、9/22 与 9/23 均返回 `NO_ACCEPTED_RUN / UNAVAILABLE`。第一次 REAL_FORWARD 预检在写入 manifest 或 Focus 业务行前因 artifact catalog 期望 SHA 与当前 normalized Parquet 不符而 fail closed。只读校验确认 Phase1 收据 `PASS`、cutoff `20260923`、generation `ace2da0b0f74436b86aaca0dd4128ab7`，adjusted/factor 两个文件的实际 SHA 均与同一最终收据精确一致；catalog 的 `AVAILABLE` normalized 条目仍绑定前一摘要 `ac248c100ddce7dd6927d7b29e601f3c45193ae94e7880957c76637d0a07f81f`。按该双文件收据精确重登记后，第二次预检越过文件身份和事实构建，随后暴露 replay gate 缺陷：`sql.Composed` 被调用 `.format()`，在提交或 staging manifest 前抛出 `AttributeError`。无 Focus 业务写入。 |
| acceptance_result | `FULL_PASS`。执行 exact-date `--apply` 后 PG run/head 为 `focus-run-f8ba1c4915d1c330506d9bc8954b0a2d`、2026-09-23、revision 1、`REAL_FORWARD / ACTIVATED / VALID`；source identity 和 manifest digest 与预检一致；写入 310 daily items/episodes/observations/evaluations、3,066 predicate facts、17 entry baskets，current projection rebuild verified。实现审计发现 run writer 曾把 manifest 中的 basis 错落为 `HISTORICAL_RECONSTRUCTED`，已修复 writer 并按 `FOCUS_RUN_BASIS_RECONCILIATION_V1` 以 run_id/source/head/item-count 全匹配条件，仅校正该 run 的 evaluation_basis 列，保留旧值和理由于独立审计记录。修复只读 API 的 PostgreSQL date/string 比较；定向回归 `10 passed`。受控服务重启 `READY`。重启后 live `/api/v3/focus-tracker/summary`、按 9/23 日期查询及 `/items` 均为 `AVAILABLE`；汇总为 V3.3 候选 273、V3 sector 17、V3 shortlist stock 20，总计 310。Focus 页面 HTTP 200，浏览器 accessibility tree 显示日期 9/23、REAL_FORWARD、首屏行及“共 310 条”。 |
| acceptance_boundary | 技术 hash 审计仍使 310 条 validity=`UNKNOWN`、path=`DATA_UNAVAILABLE`，页面按合同逐行明示，不是接口空结果；`focus_run_id` 真实为 REAL_FORWARD，但这不等于效果有效性验收。9/22 仅有历史构建/回滚演练，未接受/激活 REAL_FORWARD Focus head，日期查询返回 `NO_ACCEPTED_RUN`，不以其他日期冒充。 |
| next_stage | 继续 FOCUS-04 outcome 目标到期结算和 FOCUS-05 统计物化/性能门；技术 hash identity reconciliation 独立 fail-closed。9/23 页面当前已打开于本地关注跟踪页。 |
