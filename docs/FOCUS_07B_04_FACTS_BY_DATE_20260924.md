# 07B-04 逐日 predicate facts（2026-09-24）

## 阶段合同

依据修订方案第 20 节，`FOCUS_PREDICATE_FACTS_BY_DATE_V1` 只物化当前 AST 所需窗口和字段。每个日值来自 SHA 验证的本地标准化切片；收盘价和动态 MA 使用观察日的同一组本地仿射复权坐标。信号日冻结价格仅在来源明确为 `TDX_NATIVE_AFFINE_QFQ` 时转换到观察日坐标。缺实际行情不得前填。

## 实施与证据

- 新增 `predicate_facts_by_date.py`，输出逐日 `has_actual_bar`、`close`、动态 MA5/MA20、当日结构破坏状态及事实摘要；切片和数据窗口都由 07B-03 的要求界定。
- `rps20_delta3` 尚无经接受的逐日历史提供者，明确输出 `None`；不能拿当天候选因子或 `rps5_delta3` 冒充。
- 日构建器把事实交给失效 AST，保存需求、事实摘要和判定证据。连续失效的评估日从 episode 首日起算；信号前历史仅可用于均线形成，不能计入连续失效天数。
- 测试覆盖同坐标价格与 MA、冻结阈值重锚、缺行情和复权身份失配、首日不能触发两日失效、第二日两根实际行情可触发、缺一根则 UNKNOWN。Focus 测试 107 项通过。
- 23 日已接受来源只读重建通过：310 条观察，273 条 V3.3；validity 全为 `UNKNOWN`，写入 0，闭包摘要 `6d2da37a9c3047c4cc0c033430ca623a01c7c388749f2baeb96061b109a08967`。先前未限制 episode 首日时曾出现 51 条首日误判失效，已修正并复核为 0。

## 验收与下一阶段

07B-04 的最小逐日事实与失效评估接入已完成。`rps20_delta3` 和其他缺来源字段仍保持 UNKNOWN；07B-05 必须检查声明的适用能力与事实提供者是否一致。24 日连续正式验收由 07A 门独立执行。
