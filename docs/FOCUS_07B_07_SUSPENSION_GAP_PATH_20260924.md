# 07B-07 停牌与缺口路径拆分（2026-09-24）

## 阶段合同

依据修订方案第 23 节，价格指标使用实际成交日路径；连续会话谓词仍严格使用主交易日历。只跨过生产数据状态合同中的 `CONFIRMED_SUSPENSION`（且审计状态标记一致），不跨越 `INFERRED_GAP` 等未核实缺口。版本合同为 `FOCUS_ACTUAL_TRADED_PATH_V1`、`FOCUS_OBSERVATION_INPUT_V2`、`FOCUS_OUTCOME_TARGET_PLAN_V2`。

## 实施与证据

- 观察路径和股票到期结果共用实际成交路径投影；完整主日历、实际参与日期、停牌日期及未核实缺口日期进入摘要。观察证据和结果结算证据显式保存缺口数及日期。
- 有效首尾行情、内部已证实停牌后复牌可计算 return/MFE/MAE/drawdown；首尾无行情或中间未核实缺口仍返回不可用。`CONFIRMED_SUSPENSION` 与 `trade_status_known`、`is_synthetic_fill` 不一致时报合同错误。
- 连续失效 AST 保留主交易日历，停牌日 `has_actual_bar=false` 导致该连续谓词 UNKNOWN；没有前填。
- 合成测试覆盖复牌指标、结果覆盖率、未核实缺口、首尾停牌、审计标记一致性与连续谓词；Focus 测试 121 项通过。
- 星帅尔 `SZ.002860` 的真实 9 月 21～23 日切片：22、23 日为 `MISSING_DATA`，没有已确认停牌或复牌行情；只读价格路径 `DATA_UNAVAILABLE`，缺口 2、未核实日期 2、收益为空。它不属于 23 日 Focus episode 或冻结篮子；这只能作为未知缺口失败关闭的真实反例。全量标准化文件目前无 `CONFIRMED_SUSPENSION` 行。
- 23 日已接受来源只读构建：310 条，缺口会话 0、停牌会话 0，写入 0；生产状态合同对齐后的闭包摘要 `5654673b869f9ae8092d04c0e29e7a7066371094b4bc49ad02230f52a61ec372`。独立审计见 `docs/audits/FOCUS_SUSPENSION_PATH_GAP_20260924.md`。

## 验收与下一阶段

07B-07 的代码及合成/现有数据只读验收完成；真实复牌样本仍需后续数据验证。07B 代码项已逐项推进，07B-01 的新日对象回查及本项真实复牌样本仍待正式数据。下一阶段 07C 是算法合同变更，必须先核对修订方案第 24 节及后续约束再设计和实施。24 日正式连续交易日验收仍是 07A 独立门。
