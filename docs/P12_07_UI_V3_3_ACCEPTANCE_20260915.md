# P12-07 UI_V3_3 验收

## 阶段合同

依据 `V3_TODAY_RESEARCH_PRIORITY_DETAILED_UPGRADE_PLAN_20260914.md` 的 P12-07：在 P12-06 完整包发布门之后切换首页数据源，提供详情、完整分页、日期和空态，实查桌面与窄屏，并与新接口对账。研究功能可发布预览，效果必须继续标记 `EFFECT_OBSERVATION_PENDING`。

本阶段不进入 P12-08，不修改 TDX 输入，不写生产数据库，也不把 P12 文件运行冒充旧 `research_runs` 记录。

## 实现与证据

- 新增 `TODAY_RESEARCH_V3_3_API_01` 只读合同，列表和详情每次先验证活动指针、文件哈希和 bundle digest。指针缺失返回 `NOT_BUILT`；校验失败返回 `UNAVAILABLE` 和空结果，不读取旧候选补位。
- 新增 `GET /api/v3/research/today`，支持 1–100 条分页、类别、选择模式和证券代码筛选；新增 `GET /api/v3/research/today/{security_id}`，详情返回同一 digest 的结果与身份。
- 统一 `/v3` 首页“优先研究个股”已切换到新接口，展示真实交易日、效果状态、113 条总数和 25 条分页。旧 `/api/candidates` 保留为旧功能兼容，但不在该首页模块调用。
- `/v3/research-preview` 提供筛选和窄屏卡片诊断视图。
- 机器回执：`reports/p12_07/p12_07_stage_gate.json`。

## 验收结果

**DEGRADED_PASS**。

通过项：活动包 READY；API/页面总数均为 113；第一页 25 条且可翻页；详情 run id 与 digest 和列表相同；首页没有旧候选回退；桌面实查通过；390×844 窄屏的筛选、卡片与详情实查通过；定向回归 8 passed；Node 语法和 Python compileall 通过。

全量 V3 回归为 268 passed、17 failed。其中 16 项因当前 checkout 缺少既有 P09/P11 历史回执文件而失败；1 项连接端口 28765 上已经运行的旧服务，未加载本阶段代码。它们不改变本阶段实现结论，但全量基线不满足 `FULL_PASS`，因此保守定为 `DEGRADED_PASS`。

效果门继续保持 `EFFECT_OBSERVATION_PENDING`。P12-06 的文件运行身份仍未登记到新的版本化数据库注册表；页面现在已能安全读取文件指针，这解决了“页面尚未读取新指针”，没有伪造旧 `research_runs` 身份。

## 下一阶段

验收通过后才可进入 P12-08。本次停在 P12-07，不自动启动下一阶段。
