# P12-08A episode 身份补全验收

## 阶段合同

P12-08 首轮封存发现活动候选包没有逐行 `episode_id`，导致最低报告门无法累计独立 episode。本阶段在不改变资格、分数和活动 bundle 的前提下补全前向 episode 身份合同；旧观测保持不可变，以新合同 `TODAY_RESEARCH_FORWARD_V3_3_CANDIDATE_03` 形成独立修订。

首次进入的 `(security_id, primary_category)` 以合同版本、证券、类别和首次日期生成稳定摘要身份。同一证券同一主类别连续出现时复用身份；主类别变化、退出后再次进入时生成新身份。选择模式变化仍属于同一 episode。身份只服务冻结、迁移和后验评价，不把同日相关股票解释为统计独立。

## 证据与验收

**DEGRADED_PASS**。2026-09-14 的 113 条候选均获得唯一 episode 身份，episode 身份覆盖为 113/113。单元反例覆盖连续复用和类别变化换号，P12-07/08 定向回归 10 passed，compileall 和 `git diff --check` 通过；同输入重跑复用相同观测摘要。

当前最低门为 1/20 个真实信号日、113/50 个唯一 episode 身份。episode 数量条件已满足，但日期条件未满足；同日相关性仍须在最终报告中按日期分组处理。当前时间为 2026-09-15 盘中，生产 publication、活动包、受管日线及通达信收盘日线均只到 2026-09-14，因此没有创建虚假的第二个 PIT 日。效果状态继续为 `EFFECT_OBSERVATION_PENDING`，效果专项审计保持开启。

## 下一步

等待新的收盘 publication 和对应 V3.3 活动 bundle，随后按相同合同封存下一真实日并验证迁移。达到20个真实信号日之前不得进入 V3 校准。
