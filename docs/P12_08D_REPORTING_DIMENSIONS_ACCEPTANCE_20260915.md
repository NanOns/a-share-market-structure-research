# P12-08D 前向报告维度完整性验收

## 阶段合同

依据升级文档 §17.2，前向报告需按场景、市场强弱、行业/概念、流动性、波动、持续天数和重叠程度分层。本阶段在继续积累真实日之前扩展不可变观测字段，防止后续因少字段重新构建历史。可获得的当日事实原样冻结；活动候选包未提供的维度明确为 `UNAVAILABLE`，不推断、不补0。

观测合同升级为 `TODAY_RESEARCH_FORWARD_V3_3_CANDIDATE_04`。episode 身份改由独立且稳定的 `FORWARD_EPISODE_ID_V1` 合同生成，避免仅因观测展示字段升级而改变未来 episode 身份。旧合同观测保持不变。

## 验收结果

**DEGRADED_PASS**。2026-09-14 的113条观测全部保留 LIQ20、RPS、趋势质量、行业/概念关系数量、支持状态、类别重叠及 episode 信息。LIQ20中位数138,984,876元，范围23,500,821至3,650,678,400元；行业与概念关系覆盖均为113/113；107条命中一个类别，6条命中两个类别；113个episode当前持续期均为一个已观测会话。

市场强弱和波动字段在当前活动候选包中均为0/113，报告明确标记 `UNAVAILABLE`。它们需要未来上游 bundle 合同补充，当前观测不能事后伪造。机器阶段门为 `reports/p12_08/p12_08d_stage_gate.json`。P12-07/08/08B/08C 定向回归18 passed，compileall 与 `git diff --check` 通过。下游452条评价计划已按新观测重新生成，仍全部 `NOT_DUE`；真实收益仍未物化。

## 下一步

等待新的真实收盘数据。新的活动 bundle 若仍缺市场强弱和波动，只能继续按 `UNKNOWN` 封存；补充这两个上游字段必须另立版本化 bundle 合同，不能覆盖既有信号。达到20个真实信号日前不进入V3校准。
