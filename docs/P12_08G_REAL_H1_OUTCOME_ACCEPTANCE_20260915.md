# P12-08G 首个真实 h=1 后验结果验收

## 阶段合同

依据升级文档 §17.1、P12-08B 后验计划和 P12-08C 物化合同，本阶段只处理 2026-09-14 信号在 2026-09-15 到期的 `h=1` 结果。评价源合同为 `P12_FORWARD_EVALUATION_SOURCE_V1`：从受管 adjusted daily 中冻结涉及证券的原始高、低、收盘和实际行情标志，并从只读 TDX GBBQ 冻结信号日至到期日之间的除权事件；源文件摘要、评价源摘要和调整版本共同进入结果身份。

同一交易日存在多个不可变观察修订时，后验计划沿前向报告相同的规范选择规则，每个交易日只消费一个修订，优先选择市场强弱和波动维度更完整者。`NOT_DUE` 不消费未来数据；到期但缺实际行情时保留 `DATA_GAP`，禁止填0。

## 验收结果

**DEGRADED_PASS**。评价源摘要为 `790185116903851cc70607e64138ab77e23fa25ab7904edd8775f1f2c58efe6a`，113条到期 `h=1` 结果全部得到明确状态：112条 `OBSERVED`，1条 `DATA_GAP`，另有539条较长期限继续 `NOT_DUE`。112条观察结果具有112个唯一 materialized identity。

唯一缺口为 `SH.603400`：14日有实际行情，15日行存在但 `has_actual_bar=false` 且 OHLC 为空，因此记录 `HORIZON_WINDOW_INCOMPLETE`，没有生成收益字段。112条描述性结果的中位数为 FRET -1.5846%、MFE +2.2197%、MAE -3.1379%。这些是首个单日后验描述，不构成效果通过、概率声明或参数校准依据。

P12 相关回归通过，compileall、JSON 校验和 `git diff --check` 通过。TDX 仅只读访问，未写生产数据库。

## 下一步

本阶段完成后回到真实日观察流程。下一真实收盘日先构建第三个前向观察；届时同时按已经冻结的评价合同处理新到期期限。20个真实信号日门槛和独立效果审计保持开启。
