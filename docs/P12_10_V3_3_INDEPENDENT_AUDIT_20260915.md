# V3/V3.3 增量算法独立审计与修复（2026-09-15）

阶段合同：`P12-10_V3_3_INDEPENDENT_AUDIT_AND_REMEDIATION_V1`。审计依据为 `V3-TODAY-RESEARCH-UPGRADE-PLAN-v2.1-AUDITED`、P12-01 至 P12-09 回执、当前源码、测试、活动研究包、真实前向观测及页面实际渲染。TDX 来源只读。

## 审计发现与修复

1. **P1：日常漏斗固定要求 6,182 股。** 该数字只是 9 月 14 日冻结基线，上市、退市或范围变化会造成未来一键生成误阻断。已改为运行时全量 `stocks>0` 且所有场景三值计数与该运行全量一致；阶段验证器同步取消固定数量。
2. **P1：P12-06 验证器有生产副作用。** 验证器会重新构建旧基础包并切换活动指针，审计时真实复现，导致增强包被降级。已改为仅验证当前包，两次读取摘要一致且数据库不变；修复后运行前后指针 SHA-256 一致。
3. **P1：日常流水线可误复用基础包。** 旧复用条件只检查名称和扫描证据，P12-06 基础包也可能满足。已增加增强包合同 `TODAY_RESEARCH_BUNDLE_V3_3_CANDIDATE_02` 硬门。
4. **P1：流水线阶段输入未强制绑定调用方 publication/date，失败后活动指针可能停在中间包。** 已向子阶段传递期望身份，P12-03/P12-04 在写产物前拒绝错配；流水线保存原指针并在任何子阶段失败或最终身份不符时原子恢复。错误 publication 实测退出码1且指针不变。
5. **P2：退市证券若在调整日线中完全缺行，仍会成为 DATA_GAP。** 已在冻结评价源中为到期日缺行生成无价格状态占位；当前证券表存在为停牌，不存在为退市。没有补造 OHLC。
6. **P2：包中已有完整扫描证据，但页面没有展示。** 已增加四场景中文判定证据，分别显示已满足、未满足与待确认条件；浏览器实查新恒泰详情通过。
7. **P2：测试基础设施依赖调用方手工设置项目根路径，实时详情断言仍指向旧内容。** 已将项目根和 `src` 统一加入测试路径，并把实时断言更新为新版“场景判定证据”。

## 验收证据

- P12/V3.3 定向与实时浏览器回归：`67 passed`。
- 全量 `tests/upgrade_v3`：`292 passed, 16 failed`。16项均为从未纳入 Git、当前也不存在的旧 P09/P11 历史回执依赖；没有伪造或重建历史事实来消除失败。
- 当前活动包：`TODAY_RESEARCH_BUNDLE_V3_3_CANDIDATE_02`，publication `m4-d2223af86662ab21ee84b83d435f0785`，58行，名称58/58，扫描证据58/58，摘要 `a81dc08d2ecd1af3edea9a517bf4df13940153eaaf401b7c997500d9b4a8553e`。
- 前向门：2/20个真实信号日，163个独立 episode；效果仍为 `EFFECT_OBSERVATION_PENDING`。
- 后验结果：OBSERVED 112、SUSPENDED 1、DELISTED 0、DATA_GAP 0、NOT_DUE 539。
- P12-06/P12-07/P12-08E 当前验证均通过，验证前后活动指针哈希一致。

## 审计结论

接受结果：`DEGRADED_PASS`。本次发现的当前源码、流水线、验证器、状态分类、UI证据和测试断言缺陷已修复，当前生产活动包恢复正确且页面可用。降级只来自两个尚未满足的独立事实：真实前向信号日仍为2/20，以及16项历史P09/P11回执不在版本库且当前不存在。它们不阻断继续积累真实前向日，但禁止宣称算法效果验收完成。

下一阶段：等待用户验收本审计阶段；通过后继续按真实收盘日累计 P12-08，不进入参数校准。
