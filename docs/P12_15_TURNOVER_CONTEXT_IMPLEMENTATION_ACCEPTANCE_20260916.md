# P12-15 换手率上下文算法实施验收（2026-09-16）

## 阶段合同与前置门

- 阶段合同：`TURNOVER_CONTEXT_V3_3_V1`；参数合同：`TURNOVER_CONTEXT_PARAMS_V1`。
- 执行前读取最新设计 `P12_15_TURNOVER_CONTEXT_ALGORITHM_DESIGN_20260916.md` 和来源复测回执。
- Phase 0 权威封印为 `FULL_PASS_TDX_NATIVE`，折算项目门为 `FULL_PASS`；`phase0_closed=true`、`tdx_source_unchanged=true`。本阶段未访问或修改TDX源目录。
- 按用户范围，来源继续使用腾讯 `TENCENT_QUOTES_LATEST`，不扩展或比较新来源，不保存raw payload。

## 实施内容

新增纯函数上下文层和版本化参数：先绑定冻结候选与来源事实，再按业务组和来源身份组执行80%全局门、70%组覆盖门及最少5条门；使用中点经验分布计算LOW/NORMAL/HIGH。随后读取同研究包已封存的场景事实，生成STRONG/MARGINAL/UNKNOWN、T1/T2/T3和可解释规则证据。

未评分候选只在CONTEXT_READY的原槽位之间稳定移动；缺失、身份不足、价格事实不足、SCORED行均锁位。候选集合、核心分数、category_rank和默认核心顺序不变。服务分别返回来源事实状态与算法层状态；页面只读生成期物化数据，展示动态口径、验证状态、Tier、上下文和降级原因。

腾讯新请求保留原50只/批、8秒、零重试和本地主流程不阻断边界。适配器传递版本身份和源时间；源日期与目标日冲突直接拒绝，目标日收盘后时间可标记SESSION_VERIFIED。腾讯分母当前仍仅DECLARED_ONLY，因此在独立口径证据完成前不会生成正负语义或增强排序。

## 验收证据

- P12-14/P12-15专项回归：29项通过，0项失败。
- `tests/upgrade_v3`：300项通过、45项失败。45项均来自进入本阶段前的工作区状态：现有未提交035迁移使旧测试仍期待034为最后迁移；以及用户工作区中已删除的`reports/upgrade_v3`固定报告。未修改、恢复或覆盖这些无关改动。
- 当前真实物化样本只读回放：56只候选、47条腾讯事实，事实覆盖83.93%；旧观察缺少已核实分母和完整版本身份，可比较0条，语义覆盖0%，算法层按合同为`BYPASSED`。47条换手率仍可展示，全部保持T2/锁位，没有伪造HIGH/LOW或增强排序。

机器回执见 `reports/p12_15/P12_15_TURNOVER_CONTEXT_IMPLEMENTATION_RECEIPT.json`。

## 接受结论与下一阶段

本阶段结论为 **`DEGRADED_PASS`**：算法、服务与页面工程链已经落地并通过专项反例；真实腾讯旧观察因来源口径/身份未完成而正确中性降级，不能宣称效果通过。

`TC-AUD-01`（分母口径）、`TC-AUD-02`（历史观察时间与as-of选择）、`TC-AUD-03`（核心评分完备性）继续独立OPEN。下一阶段仅在用户决定处理来源时，补腾讯分母证据并用新元数据产生观察；效果验收仍需独立后验，不以本次测试通过替代。
