# P12-16 换手备用来源与170日数据接入验收

阶段合同为`P12_16_FALLBACK_AND_HISTORY_INTEGRATION_V1`，Phase 0门为`FULL_PASS`。

换手来源路由已调整为腾讯主源、搜狐第一备用、证券时报第二备用。腾讯按最多50只批量请求；只有腾讯批次失败时才并发请求单股备用源，最大并发8、单请求3秒、零重试，任一来源失败都不阻断本地研究包。所有来源均保留独立`source_id/source_contract_id/field_map_version`，不保存raw payload。搜狐与证券时报当前分母仍为`DECLARED_ONLY`，因此只能形成事实展示；在分母证据升级前不会进入换手Tier或增强排序。

真实小样本探测`SH.600023`：搜狐HTTP 200并解析一条换手率0.42%；证券时报HTTP 200并解析一条换手率0.42%。两者仍须逐日通过本地收盘价、成交额、成交量指纹绑定。

170日数据新增`TODAY_RESEARCH_HISTORY_170_CONSUMER_V1`读取器。读取前校验阶段FULL_PASS、manifest摘要及两个Parquet哈希；P12-03当日逐股因子入口优先消费目标日物化事实，并把数据集合同、摘要、日期和行数写入`input_identity.history_170`。真实读取2026-09-15共6,182只，摘要`35b083cc...7417`。目标日不在固定数据集时返回0行并沿既有当日计算路径运行，不把旧日数据冒充新日事实。

专项回归36项通过。阶段结果`FULL_PASS`。当前工作台进程仍需重启才能加载新增适配器和170日消费者；本阶段没有触发生产构建、修改数据库或TDX。
