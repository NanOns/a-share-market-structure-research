# P12-16 V3.3闭环审计（2026-09-16）

## 审计合同与边界

合同为`P12_16_V3_3_CLOSED_LOOP_AUDIT_V1`。本轮仅做只读审计：核对P12-11至P12-15文档、回执、活动bundle、170日Parquet、当前服务接口、页面任务路由和定向测试；没有触发页面生产构建、没有在线重抓、没有修改数据库或TDX。Phase 0当前门为`FULL_PASS`。

## 结论

总体接受结果为 **`DEGRADED_PASS / NOT_CLOSED`**。P12-11、P12-12、P12-13物化本身和页面到日常P12流水线的源码接线成立，但“170日数据已纳入日常V3.3使用”“新换手算法已在当前运行页面生效”“达到20日后效果审计”均不成立。

## 分项证据

1. P12-11数据库登记回执为FULL_PASS：活动Full LOO bundle摘要`45ae278c...fa4d7`、56条候选、数据库56条、幂等回放与payload一致。
2. P12-12 Full LOO为FULL_PASS：58条基线变为56条正式候选，完整剔除市场基准与全部重叠板块，活动指针及数据库登记一致。
3. 170日数据集完整：摘要`35b083cc...7417`，2026-01-06至2026-09-15共170日；股票1,050,940行、板块90,440行，复合键重复均为0；两个Parquet文件当前SHA-256仍与manifest一致。
4. 170日数据没有进入日常V3.3消费链：全项目引用仅存在于物化脚本、验收回执和静态测试；`run_p12_daily_pipeline.py`的阶段列表不读取`data/research_history_v3_3_170`，当前bundle只声明`LOCAL_CLOSE_ONLY_WITH_RECONSTRUCTED_HISTORY_DIAGNOSTICS`，没有绑定170日数据集摘要。因此只能认定“可供使用的数据已生成”，不能认定“已纳入候选/评分/episode/页面计算”。
5. 页面按钮源码链存在：前端POST `/api/jobs`并轮询；服务依次完成发布、M8/M9、M10、V3研究运行、P12日常流水线；P12包含Full LOO与后验阶段，成功后调用可选换手子流程。腾讯适配器是当前源码默认fetcher，50只/批、8秒、零重试。
6. 新换手上下文算法存在于源码并被服务源码调用，定向链测试41项通过。但当前运行于127.0.0.1:28765的服务仍返回旧API形态：没有`algorithm_contract/layer_status/Tier`，并返回旧全体百分位与旧增强序号。这证明服务进程尚未加载今天的模块，当前页面运行态未生效。
7. 即使重启到新源码，当前旧腾讯观察也只有47/56条事实、可比较0条、语义覆盖0%，新合同会返回`BYPASSED`。这是对未核实分母和旧身份字段的正确fail-closed；换手率可展示，但目前不会实际形成T1/T3或增强顺序。
8. 当前真实积累只有1个有效换手观察日、47条绑定记录；20个有效交易日门未达到，效果审计和参数校准尚未开始。

## 必须关闭的事项

- 明确170日基础事实的生产消费者及身份：候选生成、历史episode、诊断或后验分别绑定数据集摘要，禁止仅把目录存在当作已使用。
- 重启工作台服务并验证实时接口返回`TURNOVER_CONTEXT_V3_3_V1`、`layer_status`和逐股Tier字段；保留页面只读、不主动联网。
- 新交易日由页面生成链直接获取腾讯并封存完整版本/时间身份；旧观察不倒填。
- 腾讯分母仍未核实前保持语义旁路；累计满20个独立有效日后再做效果审计和参数校准。

机器回执见`reports/p12_16/P12_16_V3_3_CLOSED_LOOP_AUDIT_RECEIPT.json`。
