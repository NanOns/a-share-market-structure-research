# P12 日常一键生成输入修复验收

## 阶段合同

依据 `V3_TODAY_RESEARCH_PRIORITY_DETAILED_UPGRADE_PLAN_20260914.md` 的 P12-06 完整发布要求和“保留一键生成”决定，本阶段合同为 `P12_DAILY_ONE_CLICK_INPUT_REPAIR_V1`。官方日线包的上传日期只表示文件批次时间；实际经济交易日必须从解包后的日线内容推断。预期交易日由调用方明确提供时仍须严格匹配，否则失败关闭。TDX 目录全程只读。

同一个官方包可能对应多个在线元数据快照。来源目录保留原有不可变记录，并以元数据快照 ID 形成版本化目录键，禁止覆盖旧身份。恢复及校验必须优先使用 source bundle 已记录的 staging 路径，不能用经济交易日反推上传目录。

## 故障与修复

页面的“生成今日数据”调用 `/api/jobs` 并进入 `run_upgrade_m3.py`。2026-09-15 下载的官方包实际最后交易日为 2026-09-14，旧逻辑把上传日 2026-09-15 当作目标交易日，因此三个市场计数均为零并报 `DAY_VALIDATION_FAILED`。修复后，内容校验识别 2026-09-14：上海 4,857、深圳 4,484、北京 345，主要指数存在，普通 A 股非法记录为零。

后续真实链还验证了两个身份边界：source bundle 使用已记录 staging 路径；同包不同元数据快照写入版本化来源目录。发布状态 API 同时补回顶层 `publication_id`，幂等恢复会清除瞬态 `COMPUTING` 状态。

## 验收结果

**FULL_PASS**。真实页面同路径任务 `daily-f3b9a357fcc04d8683cd8eceaef2839c` 最终返回 `SUCCESS/READY`，形成 2026-09-14 发布 `m4-70f5b499f805d0897826a165c049098b`（revision 2）、M10 快照 `m10-mainline-preview-dcf87de4d69dd1cf` 和研究运行 `research-d87f87a66c354543942584c41a3ba7bc`。P12 前向 bundle 的累计真实信号日门槛和效果审计状态未因此改变。

定向测试 32 passed，compileall 和 `git diff --check` 通过。未修改 TDX 根目录。

## 下一步

恢复日常使用“生成今日数据”。若页面填写的预期日期晚于包内实际最近交易日，系统会继续返回日期不匹配并拒绝发布；不应把上传日伪装成交易日。
