# V2 Shadow Contract V1

Version `v2-shadow-contract-v1.0`. `shadow=true`; `production_eligible=false`. Shadow reads V1 canonical data and writes only `data/shadow/v2` and `reports/shadow/v2`. It cannot modify V1 outputs, priority, release receipt, or current pointer. Its computation identity is independent and binds one immutable V1 comparison baseline. `LIMIT_UP_IS_NOT_AN_EXCLUSION=true`; limit-up observations never veto a structure, score it, or penalize it. V2 remains descriptive and Shadow-only.
