# V3 市场事件页面与本地历史模块复核（2026-09-14）

## 阶段合同

- 审计项：`V3-MARKET-EVENT-UI-REVIEW`；依据最新 `WORKBENCH_DUAL_TRACK_IMPLEMENTATION_SPEC_V3` §19、P09 在线产品合同及用户页面反馈。
- 结果：`DEGRADED_PASS`；本轮页面与接口修复通过，但 EXT06 无来源时间/完整口径说明、EXT05 分页覆盖未知，不能提升整体发布门。

## 来源和用途证据

- EXT06 当日真实响应包含 `rise_fall` 的涨、跌、平、涨停、跌停，`turnover` 的当前/前值中文单位字符串，以及 `north_flow` 当前显示字符串。页面过去只展示前三项；现按来源原值增加涨停、跌停、成交额当前/前值和北向显示，缺失仍显示“暂无”，不换算或跨源合并。来源另有 `limit_up.pre/now`，但语义与 `rise_fall.limit_up` 不同且未核定，不作为“涨停数”展示。
- “历史窗口”是本项目本地 `/api/market/cycle` 的 30–250 交易日查询长度，使用发布版本/分析快照统计涨跌平、成交额、均线覆盖、新高和队列；不是龙字诀七池或 EXT06 的历史功能。
- V3 已有本地今日总览和在线事件速览，两张本地“市场广度”“量额与 MA 覆盖”图在该页面信息重复且混淆在线/本地边界；V3 不再绘制或显示。V2 历史兼容保留。
- 本地历史表暂保留作特定日期的快照追溯与单日下钻，不把它当作盘中决策主视图；是否进一步折叠/移除需独立产品裁决。

## 修复与验收

- “在线涨停明细”改用七池同款固定列宽表格，说明全文换行显示，表格区固定最高 440px 内部滚动。
- EXT06 规范化补充 `north_flow_display`；JavaScript 语法检查及 23 项定向测试通过。
- 本地市场周期 8 项回归测试通过；服务受控重启 PID 40576 → 50692 后，接口实际返回成交额 `1.6万亿`、北向来源显示 `0.0亿`，`/v3` 已引用新资源版本。
- 未改 TDX 输入、生产数据库、在线来源请求边界或本地快照身份。
- 下一阶段：独立确认 EXT06 北向值与来源更新时间语义，并按用户裁决决定本地历史表是否折叠或退出 V3。
