# V3 在线池去重与涨停时间排序（2026-09-14）

## 阶段合同

- 审计项：`V3-ONLINE-LIMIT-UP-DEDUP-SORT`；依据最新 `WORKBENCH_DUAL_TRACK_IMPLEMENTATION_SPEC_V3` P09、`V3_MARKET_EVENT_UI_REVIEW_20260914` 及用户去重裁决。
- 结果：`FULL_PASS`（去重与排序切片），不提升 EXT05 分页覆盖状态或 P09 整体发布门。

## 证据与实现

- 页面原“七池明细”涨停与下方“在线涨停明细”都调用 `/api/v3/events/pools?pool_type=limit_up`，均为 EXT05 相同来源池，只是每页条数不同，属于重复展示。七池选择器移除涨停并默认强势股；下方保留涨停唯一明细入口。
- 在线市场概况删除北向显示小卡片；EXT06 适配层既有 `north_flow_display` 不用于 V3 展示，不以来源旧值表达当前可公开资金事实。
- 在线涨停增加 `LIMIT_TIME` 显式排序模式：在完整请求时来源行上按首次涨停时间升序；缺首次时间依次用末次涨停时间、来源进入时间；三者均缺时排后，按来源连板天数降序、涨幅降序、原始来源行序及代码稳定排序。排序在分页前执行，不重写来源行或本地快照。页面显示时间与排序说明，支持 30 条分页。

## 验收与下一阶段

- 实时 EXT05 涨停池 56 条，排序后两页 30/26；当前 56 条均有首次涨停时间。
- 24 项 P09/V3 定向测试、JavaScript 语法与 diff 检查通过；新增跨页排序及无时间兜底测试。
- 服务受控重启 PID 50692 → 10620 后，真实 HTTP 响应确认 `sort=LIMIT_TIME`、总数 56；`/v3` 已默认强势股且不再含涨停重复选项。
- 未修改 TDX 输入、生产数据库、在线原始响应持久化、热榜规则或本地数据身份。
- 下一阶段：继续独立跟踪 EXT05 来源完整分页/覆盖未知的限制。
