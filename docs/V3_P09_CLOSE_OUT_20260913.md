# V3 P09 总收口

## 阶段合同

依据 V3 主文档 SHA-256 `52536035F82D4754EDA13241181F2AA8563B9D37E280E2AE39BFBD3A5A6C9D5B` 的 §18.12、§19.5、§20.8、§22.2，汇总 P09-01 至 P09-04 在线事件/产品预览及独立审计。收口合同为 `v3-p09-close-out-v1.0`。

## 结果

工程链 `FULL_PASS`，独立审计 `FULL_PASS` 且 0 findings。真实源能力按 V3 逐数据集判定：剩余探测中 EXT02、EXT03、EXT04、EXT05 七池、EXT07 四榜、EXT08 两类、EXT09 可解析；EXT06 市场概况返回来源错误，保持 `UNAVAILABLE`；既有 EXT01 收盘简图仍是 `DEGRADED`。所以 P09 总体按 V3 §22.2 收口为 `DEGRADED_PASS`，`release_ready=false`，不是代码中隐藏缺口。

## 交付证据

- 事件简图/单股证据：`/v3/events`、`GET /api/v3/events/ladder`、`GET /api/v3/events/stocks/{id}`。
- 在线总览：`/v3/online`；速览、七池、题材、分布、四榜、热门板块、热门话题和板块 online-context API 均已接入。
- 热榜、板块、话题均请求时返回，raw/row/batch 不持久化；EXT11 按 V3 §22 退役，不替换为未验证报价地址。
- 51 项 P09/M14/M7 定向回归通过，`git diff --check` 通过。
- 独立审计：`reports/upgrade_v3/P09-INDEPENDENT-AUDIT-20260913.json`，审计修复后 0 findings。
- 机器总回执：`reports/upgrade_v3/P09-CLOSE-OUT-20260913.json`。

## 下一阶段

进入 `P10-01`。EXT06、EXT01 口径/分页、严格同步盘中报价保持显式限制，后续若来源条件变化再按 V3 当前探测合同单独升级，不阻断本地链路。
