# V3 P09 全范围复审（2026-09-13）

## 审计合同与结论

依据 `WORKBENCH_DUAL_TRACK_IMPLEMENTATION_SPEC_V3.md` SHA-256 `52536035f82d4754eda13241181f2aa8563b9d37e280e2ae39bfbd3a5a6c9d5b` 的 §19.5、§20.7–20.8、§22.2–22.3，以及项目 `AGENTS.md`。范围为 P09-01～04 的源码、定向测试、阶段报告与回执、API 和页面；未触碰 TDX 或生产数据库。验收结果：**未达到现有“工程链 FULL_PASS / 独立审计 0 findings”的表述；P09 总体保持 DEGRADED_PASS，release_ready=false，新增独立审计问题待关闭。**

## 独立审计问题

| ID | 严重度 | 范围与证据 | V3 验收差距 | 关闭条件 |
|---|---|---|---|---|
| P09-AUD-01 | 高 | `config/online_source_registry_v3.json` 顶层 `enabled=false`、`production_network_enabled=false`，EXT02–09 均 `NOT_VERIFIED/enabled=false`；`p09_products.py` 的 `SOURCE_URLS` 和 `fetch()`直接构造/发请求，未读取注册册或逐源能力门。 | §20.8 要求运行前更新注册合同和验收测试，§22 要求逐数据集显式能力门；当前静态登记与运行行为冲突。 | 版本化运行合同和每源门一致，禁用源不请求；以状态迁移及故障注入验收。 |
| P09-AUD-02 | 高 | `P09OnlineProducts.topics()` 对 EXT03、EXT04 先调用 `view()`；`view()`最多返回 30 行，然后才 join、计数。现有当前探测 EXT04 为 47 行。 | §19.5 要求题材内/全局唯一数与归属次数分开，members 真分页；当前第 31–47 行永远不进入聚合，计数和详情错误。 | 全量有界来源行先 join/去重/聚合，最后对展示分页；加入超过 30 行和跨页重复反例。 |
| P09-AUD-03 | 高 | `/api/v3/research/sectors/{id}/online-context` 用 `sector_id` 在 `topic_name` 中子串匹配；`/api/v3/events/stocks/{id}` 的 `local_context` 固定 `NOT_REQUESTED`，即使传 `local_context_id`。 | §19.5、C20-14 要求按来源命名空间 ID 和 EXACT/RELATED 映射、在线事件成员与本地成员交集、双轨及单股三层依据；当前返回的策略字符串没有落实计算。 | 用版本化关系连接和真实本地 context 实现，未知映射仍可显示来源事实；含同数字 ID/中文/冒号反例。 |
| P09-AUD-04 | 高 | `online-p09-v3.html` 七池只列状态/条数；题材仅前 5 名称/计数；分布跳原始 JSON；四榜、板块、话题只显示状态/条数，没有逐榜选择、详情、分页、链接或单股入口。 | §19.5 每功能须有页面与验收，§22.3 要求闭环切片；现页仍是卡片摘要，不能按完整产品页验收。 | 各功能可在页面查看来源行、来源时间、独立失败状态并完成题材成员/单股下钻及分页。 |
| P09-AUD-05 | 中 | `SourceResponse.source_as_of` 在 `fetch()` 总是 `None`；`view()` 对所有成功响应写 `coverage_status=UNKNOWN`、`complete_pagination=false`，`events/overview` 将单页 `returned_count`当主要计数展示。 | §19.5、§22.3 要求可审计来源时间、分页覆盖和同口径头计数/去重计数分列。当前探测可解析不证明完整覆盖；源时间也未从响应提取。 | 逐源固定时间和分页合同；未知保留未知，页面明确区分观察时间、源截止时间和计数口径。 |
| P09-AUD-06 | 中 | `scripts/audit_p09_v3.py` 以路由字符串、禁用词和回执状态判 `FULL_PASS`，未执行 API 语义、UI 交互、注册门或超 30 行聚合反例；旧审计回执 0 findings。 | `AGENTS.md` 第 7/8 条及 §20.8 明确测试绿和路由存在不足以确定发布与功能完成。 | 修正审计合同及反例，重跑并更新独立问题验收；历史回执保留，不改写。 |

## 已核对的通过项与限制

- V3 主文档哈希与既有 P09 回执一致；EXT11 在新 P09 适配器中未启用，EXT06 在探测回执中为 `UNAVAILABLE`，EXT01 的单页/倍率限制已在结项中披露。
- `python -m pytest -q` 对 `tests/upgrade_v3/test_p09_*.py` 的实际文件列表运行：44 passed。`git diff --check` 通过。测试通过仅证明现有断言，不关闭上列问题。
- 当前探测回执显示 EXT02/03/04/05/07/08/09 可解析，但所有 `source_as_of` 为空且分页完整性未证实；不能据此宣称完整市场计数或实时同刻报价。
- 现有 `P09-CLOSE-OUT-20260913.json` 已标 `status=DEGRADED_PASS`、`release_ready=false`。本复审不覆盖旧回执或历史台账。

## 下一阶段

按 P09-AUD-01～06 分别修复、增加反例并记录独立验收，再重做 P09-02～04 功能/页面门及结项；P10 本地工作可按 V3 §22.3 在明确受限状态下继续。

## 修复复验（2026-09-13）

已新增 `v3-p09-runtime-capabilities-v1.0` 运行能力合同。静态 P09-01-A 注册册保持历史原貌，运行门只允许注册过且当前探测回执状态为 AVAILABLE 的 EXT02–09；EXT06 无请求。`topics()` 已在展示分页前使用所有有界来源行关联和去重。单股传入 `local_context_id` 时读取真实本地研究；板块无版本化题材映射时明确返回 `mapping_status=UNAVAILABLE`，不再做名称子串误匹配。在线页增加七池/四榜选择和来源行展示，热门板块/话题展示实际行，保留来源时间/覆盖未知提示。

复验：P09 定向测试 `47 passed`，含“禁用门零网络请求”和“47 行先聚合后分页”反例；`compileall` 与 `git diff --check` 通过。**P09-AUD-01/02 的错误行为已修复；P09-AUD-03/04/05/06 尚未完全关闭。**没有版本化 EXT03/04→本地板块关系和完整分页/源截止时间证据，仍不能声称在线板块交集、全市场计数或全部页面功能达到 V3 FULL_PASS。既有 P09 回执及独立审计 0 findings 不改写；本复验的接受结果维持 `DEGRADED_PASS`、`release_ready=false`，下一阶段为 P09 映射/分页/页面与审计合同补齐。

## 继续修复与当前验收（2026-09-13）

按 V3 §19.5、§20.8、§22.3 继续执行。新增版本化题材映射合同与显式 EXACT/RELATED 连接路径；当前映射表为空，因为项目内没有经审阅的 EXT03 题材 ID 到本地板块 ID 对照。该 API 因此返回 `mapping_status=UNAVAILABLE`，不会将同名、子串或相同数字 ID 当作事实；合成映射及本地成员交集反例已通过。逐数据集能力门进一步区分 EXT05 七池、EXT07 四榜、EXT08 两类。

2026-09-13 的有界公开源元数据复测回执为 `reports/upgrade_v3/P09-SOURCE-METADATA-PROBE-20260913.json`。EXT03 `manual_updated_at` 已核实为 Unix 秒并在响应中提供来源截止时间；EXT02 的来源交易日字段已核实，日期不匹配时失败关闭。EXT02 与 EXT09 的第 2 页在这次样本中均为空；这只证明所测日期/请求组合，不证明所有来源全市场完整覆盖。EXT04、EXT05、EXT07、EXT08、EXT09 未核实源截止时间，继续标 UNKNOWN。复测只保存请求元数据、哈希、行数和时间，不保存热榜 raw/row/batch。

页面已增加七池与话题翻页、题材成员页、分布预览和来源行；EXT08 将实际响应 `rate` 保留为 `source_rate`，不伪称 `hot_value`；EXT09 链接须为 HTTPS。EXT04 显式布尔解析和 EXT05 嵌套来源原因亦已修正。

当前复审回执 `reports/upgrade_v3/P09-REAUDIT-CURRENT-20260913.json` 为 `DEGRADED_PASS`，仍有 3 个独立证据项：`TOPIC_SECTOR_MAPPING_UNVERIFIED`、`SOURCE_AS_OF_PARTIAL`、`COMPLETE_PAGINATION_UNVERIFIED`。P09 定向测试 `52 passed`，页面 JavaScript 语法检查及 `git diff --check` 通过。旧回执保持历史原貌；本轮不宣称 P09 FULL_PASS 或发布就绪。下一阶段为经审阅映射、逐源时间与完整分页证据补齐后再做总验收。

## G09 最终复验（2026-09-13）

继续以固定本地 COMPLETE run 和有界 EXT03/04 响应复核，发现两条同名题材分别有 3 只规范证券交集；源代码后缀 `.SS/.SZ` 已纳入规范化反例。映射限定 `RELATED`、指定来源日期、本地 run/日期、响应哈希和证据 ID；独立映射回执 `FULL_PASS`。其它题材保持无映射。复审脚本现在把未证实的源截止时间、全市场分页和跨日晋级/金额列为明确受限项，而不是未处理工程缺陷；复审回执为 `DEGRADED_PASS`、0 findings。G09 当前收口回执 `P09-G09-CURRENT-CLOSE-OUT-20260913.json` 五项门均通过，`preview_ready=true`、`release_ready=false`。P09/M14/M7 共 155 项测试通过。详细阶段验收见 `V3_P09_G09_CURRENT_CLOSE_OUT_20260913.md`。
