# P11-01 存储止增长门（2026-09-13）

## 阶段结论

`P11-01` 的存储止增长门已通过，机器状态为 `FULL_PASS`。这是对已完成回执的独立
门控汇总，不改写 P11-01 初始联合验收回执，也不把物理回收或旧表删除写成止增长结果。

机器回执：[P11-01-STORAGE-STOP-GROWTH-GATE-20260913.json](../reports/upgrade_v3/P11-01-STORAGE-STOP-GROWTH-GATE-20260913.json)

## 通过依据

- P11-01 同输入重复探针证明重复运行新增 0 业务事实、0 身份/日志行、0 重复物理内容，生产库前后 size/mtime 不变。
- P11-02 已证明六个首轮迁移域旧 writer 非定义调用点为 0，旧读 API、resolver、result binding 保持可读。
- 两个辅助旧 writer 已形成单一、明确的有界边界；它们不属于迁移域停旧写失败，且旧表按用户决定保留。
- `P11-02-AUD-STORAGE-01` 已完成 52/52 `storage_objects` 逐对象引用图对账：34 个当前发布引用，17 个 stale flag 均有历史 slice provenance，1 个缺失物理文件是已记录 deleted tombstone，未发现无图引用。
- P11-03 已如实登记授权回收结果，旧表保留决策已独立登记，未删除任何表。

## 当前入口与安全边界

当前 `runtime/workbench_entry.json` 仍保持正式入口 `/v2`，旧 `/view` 回退入口仍保留。
目标 P11-04 入口为 `/v3`，但本门不执行入口切换，回执明确记录
`switch_executed=false`，以便把入口变更作为独立、可核验动作处理。

本门仅读取回执、入口 JSON 和生产数据库文件 stat；没有写生产库、写入口、访问/修改
`D:/new_tdx`、创建备份、执行 cleanup 或删除旧表。

## 未混淆事项

- P10-03 仍为 `EFFECT_OBSERVATION_PENDING`，本门不宣称策略效果。
- 旧表清理继续等待 V3 全部开发完成、旧页面功能迁移完成和逐表兼容回归。
- 用户此前授权删除的物理备份已由 P11-03 完成；本门不新建替代备份。

## 下一阶段

进入 P11-04 主入口交接：先复核最终交接清单，再由独立动作决定是否将默认入口从 `/v2`
切换至 `/v3`；切换后仍需保留旧研究工具回退入口和效果观察状态。
