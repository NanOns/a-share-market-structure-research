# V3 页面与数据集成整改记录（2026-09-13）

## 阶段合同

- 依据：`WORKBENCH_DUAL_TRACK_IMPLEMENTATION_SPEC_V3.md` §8、§18、§21、§22，以及 `WORKBENCH_TARGET_VERSION_ONLINE_EVENT_REDESIGN_PLAN_V1.md`。
- 范围：研究首页中文化与空态解释、M10 中期主线构建、在线事件在市场页集成、在线涨停同源直读、技术诊断范围收窄，以及本地今日总览回归。
- 边界：不修改 TDX 源；不使用本地涨停估算；热榜只在请求时读取且不保存 raw/row/batch/history；单日数据不足不得伪造成多日周期结论。

## 证据与接受结果

- M10 构建：`m10-mainline-preview-e982c792bc5f623d`，交易日 `2026-09-11`，553 个板块、553 行。
- 历史限制：本地仅保留一个交易日，因此全部状态为“数据不足”；页面必须解释 3/5/10/20 日状态所需历史，不能填 0 冒充结论。
- 在线涨停：V3 直接读取 EXT05 龙字诀同来源在线涨停池，页面显式标来源，不落盘，不回退本地。
- 页面：内部英文枚举改为中文业务标签；独立在线页入口从主流程移除，市场页自动加载速览、题材、热度和涨停明细。

## 2026-09-13 第二轮页面校正

- 恢复“技术状态”入口。默认只展示核心研究与支持观察；“诊断参考”不再等同于所有未命中结构的股票。
- TECHNICAL_DIAGNOSTIC_SCOPE_V2：诊断参考必须数据有效，并满足 RPS20 不低于 0.90，或同时满足多头排列、20日收益不低于15%、额比前20不低于1.20。
- V3 涨停明细直接使用龙字诀同来源在线池，不再先读本地归档梯队，也没有本地回退入口。
- 最强题材展示全部13项，题材卡可展开成员；七池独占整行；四个人气榜并列四卡展示。

## 当前接受状态

`DEGRADED_PASS`：本地单日功能与页面集成已完成；中期主线真实数据已生成但因只有单日历史不能形成持续/加速等多日分类。在线来源以实时能力独立成败，失败不阻塞本地主流程。

## 2026-09-13 第三轮首页与交互校正

- 阶段合同：保留 V1 本地今日总览及领先板块，与 V3 当前强势资格分开；V3 不再展示联动筛选菜单，仍保留底层关系数据和 V2 兼容功能；板块、个股点击改为就地详情弹窗。不改 TDX 输入及既有快照身份。
- 证据：`/api/dashboard?include_analysis=1&basis=RECONSTRUCTED` 返回 2026-09-11 领先行业 133 项、领先概念 268 项，各首页显示前 10 项；`/api/market/cycle` 同日返回有效报价 5532、上涨 639、下跌 4856、平盘 37。API30 市场摘要卡本身不可用，首页明确采用独立 M13 本地市场周期结果，不伪称 API30 已恢复。
- 页面验收：V3 首页可见交易日/数据日期、本地市场卡及领先板块，顶部菜单无联动筛选，旧兼容明细在 V3 隐藏；板块详情从 V3 研究接口读取全部成员，个股详情从本地 insight 接口读取。V2 联动 API、数据和页面仍保留。
- 接受结果：`DEGRADED_PASS`。本地领先排名为过去 20 日相对强度，并非当日上涨榜；在市场整体下跌日，领先板块当日中位涨幅仍可为负。下一阶段应独立审计 API30 市场摘要缺失与本地板块排名口径。

## 下一阶段

完成浏览器回归；若需要形成有效中期主线分类，必须另行允许构建至少 10 个历史交易日。

## 2026-09-13 第四轮：弹窗、V1 优先研究与算法审计登记

- 阶段合同：按 V3 主规格 §2、§5–8、§18 及用户新要求，修复首页详情弹窗和 V1 优先研究名单，诊断 CURRENT/POTENTIAL 与两个个股清单的输入历史；不在本轮擅自放宽阈值或重写算法，不修改 TDX 源。
- 证据：板块详情成员 API 单页最大 50，旧弹窗请求 200 导致失败；修正为 50 并在弹窗中按需加载更多。V1 `candidate_daily` 共 1,071 个，本地排序已独立接入首页，每页 20 个可翻页查看全部。
- 历史审计：调整日线 8,785 日期，`sector_cycle_daily` 仅一个日期；V3 531 板块 CURRENT=3、POTENTIAL=0，短名单 CURRENT=9、EARLY=0；三个板块 3 日变化字段均为 NaN，构建器的 10 日曾为 CURRENT 仍为常量未知，episode 仅单行。详见 `FULL_ALGORITHM_LOGIC_EXTERNAL_AUDIT_REGISTER_20260913.md` 中独立审计项 AUD-HIST-01 等。
- 验收：浏览器实际点击板块、当前关注个股与 V1 优先研究个股，详情均加载。**页面修复 DEGRADED_PASS；潜在轨道有效性 BLOCKED**，不能把 0 解读为全市场没有机会。下一阶段须先补齐历史派生与状态绑定，再运行外部算法审计矩阵。
