# V4-01/02 R4 执行证据摘要 — 2026-09-25

## 基线与提交

- 当前实现提交：`bf0f0f07fe0f727bb415eacf3aa58494f1c4e074`（在 `0e40fa5` 的 R4 实现上修正冻结快照时间戳重放条件）。
- 依据：R4 外部审计/续办说明，技术基线为 REV2；本摘要只报告可重放执行证据，不代替在线模型外部验收。
- 凭据未写入代码、命令记录或回执。

## R4 产物和观测

1. V4-01 bootstrap 在 `0e40fa5` 上完成实际 ZIP 与解包逐文件校验：包 SHA-256 `b6b88d777c74f302376513bad35e9c0e35284a65bc2d9826be25accf4d58807f`，12,438 个 `.day` 文件、29,768,339 条记录；18 个文件校验失败，状态 `BLOCKED`。回执：`reports/v4_01/v4_01_stage_receipt_R4_20260925.json`。
2. 在当前 HEAD `bf0f0f0` 上完成全量来源选择：12,508 个证券键、22,853 个选择片段、29,690,462 行；本地来源 29,341,345 行，包源补缺 349,117 行。来源包身份及 ZIP/解包验证通过，来源文件异常 19 项被保留为异常并排除出选择。
3. 00D A_STOCK 对照重放：复算 2,439/2,439 个 `TOLERATED_SOURCE_REFRESH_VOLUME_REVISION` 样本，全部选中本地记录，最大成交量差 30；选择集成 `PASS`。冻结本地快照复制晚于 00D，mtime 是复制时间而非原只读目录的历史 mtime；回执绑定此前已接受 00D 报告哈希，重放只重新核对差异分类/数量及本地优先结果。
4. BaoStock R4 边界探针在当前 HEAD 上 17 次请求、0 失败：基本信息目录 8,981 条、全市场目录 7,380 条，`sh.`/`sz.` 前缀可见、`bj.` 为 0；官方新旧代码 `bj.920799`/`bj.830799` 直查返回空基本信息，历史请求错误码 `10004011`。这限定为已探测代码形式，不扩展为所有北交所接口行为结论。历史查询确认真实停牌/复牌样本。`sz.000016` 的 10 行 `tradestatus=0` 空成交量/成交额被保留为 null。
5. 当前 HEAD 的 B6 重跑在登录阶段超时（30.376 秒，1 次请求、0 行），状态 `B6_BLOCKED`；不得据此启用 `BOUND_STRICT`。R3 已有的 50×15 成功诊断仅作为前次提交历史证据，不冒充本次复验。
6. `tests/v4_phase0 -q`：95 passed、2 skipped、0 failed。当前提交绑定回执：`reports/v4_baostock/BAOSTOCK_R4_TEST_RECEIPT.json`。

## 阶段合同与门禁

- 来源选择规则：日期重叠由本地 TDX 当前链优先，包源补本地缺口；未映射 `canonical_security_id` 保持 null；保留所选记录、替代源记录摘要/哈希、文件修订和选择理由。
- 阶段状态：来源选择已构建并通过指定的 00D 集成对照；V4-01 整体仍为 `BLOCKED / NOT COMPLETE`。
- 阻塞项：未完成 canonical identity 映射；19 项来源异常未解决；历史证券生命周期与 PIT 可评估宇宙待实现/验收；复权/原始记录口径、as-recorded 历史策略待实现/验收。B6 本次登录超时，独立容差验收未签发。
- V4-02 正式 canonical 重建、正式期间、时间泄漏与涨跌停门禁尚未开始；V4-03 入口未获授权。

## 下一步

1. 继续 V4-01：canonical identity、历史生命周期与 point-in-time 可评估宇宙、调整/未调整历史策略和经验验收；处理异常来源并保留输入原样。
2. 完成 V4-01 正式阶段验收后，开始 V4-02 formal canonical rebuild、formal period、temporal leakage 与 price-limit gates。
3. 外部验收由在线模型独立处理；本摘要和测试通过不代表阶段放行。
