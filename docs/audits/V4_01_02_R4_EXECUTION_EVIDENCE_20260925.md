# V4-01/02 R4 执行证据摘要 — 2026-09-25

## 阶段合同与结论

- 当前代码 HEAD：`5d13a22aefffa31db407274cad8695382e75601d`，分支 `codex/v4-system-reform`。
- 按 R4 执行顺序完成来源选择、BaoStock 目录/生命周期探测、挂牌日期边界抽样和历史日宇宙重建；这些探测产物均不授予阶段完成权限。
- V4-01：`BLOCKED / NOT COMPLETE`。V4-02：尚未开始，受 V4-01 门禁阻止。V4-03：未授权。
- 在线模型独立验收仍待处理；本地测试或回执不代表在线验收。
- BaoStock 凭据没有写入代码、文档、回执或请求账本。

## 已验证证据

1. **本地历史包完整性**：提交 `0e40fa5` 的 ZIP 与解包逐文件核对，包 SHA-256 `b6b88d777c74f302376513bad35e9c0e35284a65bc2d9826be25accf4d58807f`；12,438 个 `.day` 文件、29,768,339 条记录；18 个文件校验失败，原始输入保留，bootstrap 状态 `BLOCKED`。回执：`reports/v4_01/v4_01_stage_receipt_R4_20260925.json`。
2. **来源选择**：12,508 个证券键、22,853 个选择片段、29,690,462 行；本地链 29,341,345 行，包源补本地缺口 349,117 行。00D 的 2,439/2,439 个 `TOLERATED_SOURCE_REFRESH_VOLUME_REVISION` 样本重放一致，全部选择本地行，最大成交量差 30。选择集成检查通过；38 项源异常保留并排除选择。清单 SHA-256 `67771e4d5a11d392dd0cc2d0c3c9e881a9e6e7f93ba14af4783bf1bcbd0d78da`，回执及清单位于 `reports/v4_01/`。
3. **BaoStock 目录和生命周期**：安装隔离 SDK 后取得基本信息 8,981 条，`type=1` 候选 5,559 条；三个历史日期的 type=1 重建计数与当日目录逐证券完全吻合（5,100、5,157、5,222，缺失/多出均为 0）。事实文件 SHA-256 `ecdd48aa781e9160b139f6ffc6ddd85f48308b0a9ba1a7f8e5ae1f860df68c61`，探针状态仅为 `PROBE_PASS_ACCEPTANCE_PENDING`。
4. **挂牌/退市边界抽样**：6 个分层案例、16 个日期目录查询、18 次请求、无请求失败；IPO 3/3 通过，但退市 3 例中只有 1 例符合 `outDate` 当日仍在目录的假设。因此不把 `outDate` 解释成统一最后在册日；边界日期隔离为未知。探针状态 `BLOCKED`，SHA-256 `89bb2561dc4c1008e059c6c9fdbd9c322978ff917cce3cc3ced0c1d8facd38fc`。
5. **历史日宇宙**：从 2023-07-04 暖机到 2026-09-24，共 786 个代理交易日、4,036,160 条日成员记录；源行情可评估 4,027,002 条；129 条挂牌/退市边界未知。全文件独立回读通过，日摘要根 `791a0d6af7be924bdfe881e9079ddf70c51992ca9bde828672dd52428c8e49ee`，gzip SHA-256 `6b98477aa150784113979539f39415d1aeff0116138b738057e543fdfa74f075`。日交易状态未观测且未推断；613 个 `bj.` 源证券键缺少 BaoStock 生命周期/日期目录对应，canonical ID 保持未映射。回执 `reports/v4_01/historical_evaluable_universe_receipt_R4_20260925.json` 状态 `BLOCKED`。
6. **BaoStock 请求边界**：R4 样本目录中 `bj` 返回 0 条；探测的新旧代码 `bj.920799`/`bj.830799` 基本信息为空，历史请求错误码 `10004011`。该结论只适用于这些已探测代码。成功样本保留了停牌行和空成交字段为 null。
7. **B6 与测试**：当前实现的 B6 在登录阶段超时（30.376 秒，1 次请求、0 行），状态 `B6_BLOCKED`；不接受严格容差放行。当前代码上的 `tests/v4_phase0 -q` 为 95 passed、2 skipped、0 failed，回执 `reports/v4_baostock/BAOSTOCK_R4_TEST_RECEIPT.json` 绑定上述 HEAD。日请求账本记录 2026-09-25 共 456 次，低于系统软限 40,000、硬限 45,000 和供应方 50,000；不含凭据。

## 当前验收状态与下一步

- **门禁结果：BLOCKED。** 虽然三个横截面目录交叉核对通过，退市边界语义不一致仍需独立解决；北交所 613 键生命周期/日目录覆盖缺口、逐日交易状态未知、正式交易日历、身份与历史口径、复权/涨跌停验收也未完成。另有 18 个 bootstrap 文件及 38 项来源异常留待审计闭环。
- 不得将 R4 探测、95 项测试或局部来源选择通过写成 V4-01 完成。V4-02 正式 canonical 重建、时间泄漏和涨跌停门禁尚未启动。
- 后续先针对上述独立阻塞项补证据和验收；仅当 V4-01 全门禁通过后才进入 V4-02。外部验收由在线模型处理。
