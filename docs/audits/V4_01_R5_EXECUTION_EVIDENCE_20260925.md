# V4-01 R5 执行证据摘要 — 2026-09-25

## 阶段合同与结论

- 当前工作分支：`codex/v4-system-reform`；执行开始时 HEAD：`fd3941f2bd6099ee934de88a7dfce23b1796ea03`。
- 本轮只处理 REV2 §78 归属 V4-01 的稳定身份、逐日名册、BSE 生命周期、正式生命周期事实、历史成员、源异常、历史口径和回执。
- V4-01 暂定结论：`BLOCKED / NOT COMPLETE`。不使用 `DEGRADED_PASS`。
- V4-02：`BLOCKED / NOT STARTED`。复权、正式日历/交易状态、周月周期、AS_OF、时间泄漏、涨跌停均不属于 V4-01 门禁。
- 外部在线模型验收尚未执行；本地代码、测试和回执不等价于外部验收。
- BaoStock 本轮使用已安装的隔离 SDK 和公共匿名路由。请求账本不包含凭据。
- R4 `CANONICAL_SOURCE_SELECTION_V1`、local-first precedence 和 00D 重放由 R5 外部审计认可通过；其余 R5 门禁独立判定，不继承该项 PASS。

## R5 已形成的实现和证据

1. **稳定证券实体身份**：新增 `SECURITY_ENTITY_IDENTITY_V1`。证券 ID 与源证券代码分离，实体身份绑定交易所、生命周期锚点和挂牌日期；BSE 旧/新代码作为同一实体别名，符号复用按不同生命周期分配新 ID。身份生成与代码复用测试通过。
2. **BSE 源键逐项闭合盘点**：覆盖全部 613 个 BJ 源键：248 个旧代码和 248 个新代码被候选映射到同一稳定实体；16 个是非股票代码族候选；101 个 `BJ.920xxx` 保留明确的未解决原因；未解释键为 0。该项仍阻塞，因为新代码生命周期和非股票类型尚未全部通过独立官方证据验收，映射页原始内容直接访问返回 HTTP 403。
3. **正式生命周期历史表**：新增 append-only `v4.security_lifecycle_history` 迁移及 R5 字段合同，数据库中已追加 6,053 条候选事实，5,805 个候选实体；source-revision 绑定和时间戳违规数均为 0。源身份图仍是候选，正式事实验收维持 BLOCKED。
4. **精确历史名册**：按 R4 冻结的 786 个日期调用 BaoStock `query_all_stock(day)`。R5 汇总回执现为 `BUILT`：786/786 日期、4,674,161 行，逐日代码集合、排序、唯一性及摘要校验通过；原子发布文件 SHA-256 为 `185a9ba8ae99d82e04ee9ec17ca16463433906c6b646ab3b8e2aa11d242e465b`。该项仅代表数据集完整构建，不授权阶段完成。
5. **R5 历史 Universe 重建器**：使用完整逐日名册、身份图及 TDX bar 日期覆盖生成逐日成员；缺失身份保留为未解决成员，BSE 候选保留候选来源标注，不将未知行推断成已验收 A 股。
6. **历史口径**：`V4_01_HISTORY_LINEAGE_POLICY_V1` 冻结 `pre-V4 = RECONSTRUCTED_CORRECTED`、`V4 go-forward = PIT_OBSERVED_AS_RECORDED_APPEND_ONLY`。策略回执 PASS 只表示不伪造历史 as-recorded，不表示旧历史是当时观察所得。
7. **阶段门归属**：从 V4-01 PASS 要求和历史 Universe 阻塞项中移除调整行情、正式日历、正式交易状态、周月周期、AS_OF、时间泄漏和涨跌停门禁。
8. **本地测试**：最终候选 HEAD 的 `tests/v4_phase0` 结果为 103 passed、2 skipped、0 failed；R5 脚本编译通过。测试回执绑定代码 HEAD；本地测试不替代外部验收。

## 当前未决门禁

- 历史 Universe 回执为 `BLOCKED`：4,191,020 条成员记录中有 4,364 条身份未解决，另有 14,527 条源 bar 缺失（0 条 bar 校验错误）；身份未决阻止 Universe 通过。
- BSE 仍有 101 个未解决 `BJ.920xxx` 核心股票身份；16 个非股票类别仅是候选。对应报告：`reports/v4_01/bse_source_closure_receipt_R5_20260925.json`。
- `security_lifecycle_history` 中的 R5 行仍是候选事实；需要身份来源验收后才能把数据库存在等同正式接受。
- 38 条来源异常均保留；12 条已由 BaoStock 类型验证，26 条仍为代码族候选，核心 A 股未解释数为 0。候选分类不计最终闭环。
- 精确逐日名册已构建，但最终 V4-01 阶段回执仍为 `BLOCKED`；不得降级为 `DEGRADED_PASS`。

## 下一步

按 BSE 独立官方证据、稳定身份图和 26 条来源异常候选的未决项制定闭合证据；在此之前保持最终阶段 `BLOCKED`。V4-02 在 V4-01 `PASS` 且在线模型独立验收前不得开始。


## 最终执行补记（2026-09-26）

- 花名册汇总器以五个连续分片完成覆盖校验并原子生成最终数据集；786 个日期覆盖 2023-07-04 至 2026-09-24，总计 4,674,161 行。请求账本记录分片增量 777，总请求数 1,428，未触及每日 40,000 系统软上限或 45,000 系统硬上限；BaoStock 声明的 50,000/日上限也未触及。跨上海午夜的 38 次计数按保守上界记入 2026-09-26，可能高估而不会低估。
- 来源异常分类：38 条/19 个源键；12 条来源类型已验证，26 条代码族仅为候选，核心 A 股未解释异常 0；状态 `BLOCKED`。
- 历史 Universe：786 个日期，4,191,020 条成员记录，4,186,656 条身份已解析、4,364 条未解析；191,423 条 BSE 候选成员；14,527 条源 bar 缺失、0 条源 bar 校验错误；状态 `BLOCKED`。
- 最终阶段仍为 `BLOCKED`。未解决项包括稳定证券身份映射、BSE 官方生命周期独立闭合、正式生命周期事实验收、26 条异常候选及 4,364 条 Universe 身份未决。`DEGRADED_PASS` 不允许；V4-02 仍未启动。在线模型验收保持单独待处理。
